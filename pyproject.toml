[build-system]
build-backend = "poetry.masonry.api"
requires = ["poetry>=0.12"]

[tool]
[tool.poetry]
authors = ["mathmatcher <micah@tambour.io>"]
description = ""
name = "mastermind"
version = "0.1.0"
[tool.poetry.dependencies]
pop = "^12.0"
python = "^3.7"
[tool.poetry.dev-dependencies]
